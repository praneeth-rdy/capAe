import { StyleSheet } from 'react-native';
import DesignParams from '../DesignParams';

const Styles = StyleSheet.create({
  recordsContainer: {
    padding: 10
  },
  recordsHeadingContainer: {
    padding: 5,
  },
  recordsHeadingText: {
    fontSize: 18,
    fontWeight: 600,
  },
});

export default Styles;
